{
	"dependencies": {
		"@supabase/supabase-js": "^2.98.0",
		"firebase": "^12.10.0"
	},
	"devDependencies": {
		"vitest": "^4.0.18",
		"@vitest/coverage-v8": "latest"
	},
	"scripts": {
		"test": "vitest run --coverage",
		"test:watch": "vitest",
		"deploy:publish": "supabase functions deploy publish-to-firebase --project-ref kywafnfxmugjwhykwiae --no-verify-jwt"
	}
}
