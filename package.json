{
	"dependencies": {
		"@supabase/supabase-js": "^2.98.0",
		"firebase": "^12.10.0"
	},
	"devDependencies": {
		"@vitest/coverage-v8": "^4.0.18",
		"vitest": "^4.0.18"
	},
	"scripts": {
		"lint": "node -e \"console.log('Lint is not configured yet.');\"",
		"typecheck": "node -e \"console.log('Typecheck is not configured yet (no tsconfig).');\"",
		"build": "npm exec vite build",
		"test": "vitest run --coverage",
		"test:watch": "vitest",
		"deploy:admin-bootstrap": "supabase functions deploy admin-bootstrap --project-ref kywafnfxmugjwhykwiae --no-verify-jwt",
		"deploy:admin-save": "supabase functions deploy admin-save --project-ref kywafnfxmugjwhykwiae --no-verify-jwt",
		"deploy:publish": "supabase functions deploy publish-to-firebase --project-ref kywafnfxmugjwhykwiae --no-verify-jwt"
	}
}
